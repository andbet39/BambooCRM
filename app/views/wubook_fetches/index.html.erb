<p id="notice"><%= notice %></p>

<h1>Wubook Fetches</h1>

<table>
  <thead>
    <tr>
      <th>Outcome</th>
      <th>Raw</th>
      <th>Processed</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @wubook_fetches.each do |wubook_fetch| %>
      <tr>
        <td><%= wubook_fetch.outcome %></td>
        <td><%= wubook_fetch.raw %></td>
        <td><%= wubook_fetch.processed %></td>
        <td><%= link_to 'Show', wubook_fetch %></td>
        <td><%= link_to 'Edit', edit_wubook_fetch_path(wubook_fetch) %></td>
        <td><%= link_to 'Destroy', wubook_fetch, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Wubook Fetch', new_wubook_fetch_path %>
